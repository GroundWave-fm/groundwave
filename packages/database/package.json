{
  "name": "@groundwave/database",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL DDL Migrations & Database Utilities for Groundwave",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "migrate": "tsx src/migrate.ts",
    "migrate:reset": "tsx src/migrate.ts --reset",
    "seed": "tsx src/seed.ts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "h3-js": "^4.5.0",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@groundwave/config": "workspace:*",
    "@types/pg": "^8.11.11",
    "tsx": "^4.23.15",
    "typescript": "^5.8.2"
  }
}
